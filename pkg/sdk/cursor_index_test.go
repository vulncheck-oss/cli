package sdk

// LIVE TESTS - these tests require a valid token to run
// func TestGetCursorIndex(t *testing.T) {
// 	t.Run("cursor index is retrieved", func(t *testing.T) {
// 		client := Connect("URL", "")
// 		resp, err := client.GetCursorIndex("exploits", "")
// 		if err != nil {
// 			t.Error("error getting cursor index")
// 		}

// 		assert.Equal(t, X, resp.Meta.TotalDocuments)
// 	})
// }
